<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pdd.mall.mapper.FavoriteMapper">
    <resultMap id="BaseResultMap" type="com.pdd.mall.entity.Favorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM favorite WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT * FROM favorite WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert" parameterType="com.pdd.mall.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, product_id) VALUES (#{userId}, #{productId})
    </insert>

    <delete id="deleteByUserIdAndProductId">
        DELETE FROM favorite WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>
</mapper>
