<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pdd.mall.mapper.OrdersMapper">
    <resultMap id="BaseResultMap" type="com.pdd.mall.entity.Orders">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="address_id" property="addressId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findByUserIdAndStatus" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} AND status = #{status} ORDER BY create_time DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="com.pdd.mall.entity.Orders" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, address_id, total_amount, status)
        VALUES (#{orderNo}, #{userId}, #{addressId}, #{totalAmount}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE orders SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <!-- 查询超时的待付款订单（状态 0，创建时间超过指定时间） -->
    <select id="findTimeoutOrders" resultMap="BaseResultMap">
        SELECT * FROM orders 
        WHERE status = 0 
        AND create_time <![CDATA[<]]> #{expireTime}
        ORDER BY create_time ASC
    </select>
</mapper>
